
// preset form values if we receive a userdata object //
- user = typeof(udata) != 'undefined' ? udata : { }
// store the userId on the client side in a hidden input field //
input(type='hidden', value= user._id)#userId


#account-form-container.center-vertical
	form(method="post")#account-form.form-horizontal.well
		h2
		h6#sub1 
		

		hr
		h6#sub2
		.form-group
			label.control-label.col-sm-3 用户名
			.col-sm-9
				input.form-control#user-tf(type='text', name='user', value= user.user)

		.form-group
			label.control-label.col-sm-3 密码
			.col-sm-9
				input.form-control#pass-tf(type='password', name='pass', value='')

		hr

		.form-group
			label.control-label.col-sm-3 Email
			.col-sm-9
				input.form-control#email-tf(type='text', name='email', value= user.email)

		.form-group
			label.control-label.col-sm-3 推荐人
			.col-sm-9
				input.form-control#up_user(type='text', name='up_user', value= user.up_user, disabled="disabled")

		input.form-control#up_user_hidden(type='hidden', name='up_user', value= user.up_user)

		hr

		.form-buttons
			button(type='button')#account-form-btn1.btn.btn-default
			button(type='submit')#account-form-btn2.btn.btn-primary
		.clearfix



// display form errors in a custom modal window //

include modals/form-errors
